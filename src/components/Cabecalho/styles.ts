import styled from 'styled-components'

export const StyleHeader = styled.header`
  background-color: ${(props) => props.theme.corSecudanria};
  color: ${(props) => props.theme.corPrincipal};
  text-aling: center;
  padding: 24px;
`
